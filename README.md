# Виселица (Hangman) на Go

Консольная игра "Виселица" (Hangman), написанная на Go. Игроку нужно угадать случайное слово из файла, вводя буквы по одной.

## 📁 Структура проекта

```
HANGMAN/
├── cmd/
│   └── main.go                    # Точка входа
├── internal/
│   ├── resources/
│   │   └── words.txt              # Слова для игры
│   └── services/
│       ├── gameService/            # Логика игры
│       │   └── gameService.go
│       ├── menuService/             # Управление меню
│       │   └── menuService.go
│       ├── printService/            # Вывод в консоль
│       │   └── printService.go
│       ├── readFileService/         # Чтение файлов
│       │   └── readFileService.go
│       └── readFromConsoleService/  # Ввод с консоли
│           └── readFromConsoleService.go
├── .gitignore
└── go.mod
```

## 🚀 Установка и запуск

### Требования
- Go 1.16 или выше

### Быстрый старт

```bash
# 1. Клонируйте репозиторий
git clone <url-репозитория>
cd hangman

# 2. Убедитесь, что файл со словами существует
# Файл должен быть по пути: internal/resources/words.txt
# Пример содержимого:
# кошка
# собака
# программирование
# виселица

# 3. Запустите игру
go run cmd/main.go
```

### Сборка исполняемого файла

```bash
# Windows
go build -o hangman.exe cmd/main.go

# Linux/macOS
go build -o hangman cmd/main.go

# Запуск
./hangman        # Linux/macOS
hangman.exe      # Windows
```

## 🎮 Как играть

1. **Главное меню**
   ```
   1. Начать новую игру
   2. Выйти
   ```

2. **Процесс игры**
   ```
    Ход номер:  0 Ошибок осталось:  6
    Ваше слово:  * * * * * * 
    Введите букву: 
   ```

3. **Правила**
   - Вводите по одной букве
   - Угаданные буквы открываются
   - Неверные буквы уменьшают счётчик попыток
   - Победа: открыты все буквы
   - Поражение: кончились попытки

## 🏗️ Компоненты

| Сервис | Что делает |
|--------|------------|
| **gameService** | Основная логика игры |
| **menuService** | Меню и навигация |
| **printService** | Вывод в консоль |
| **readFileService** | Чтение слов из файла |
| **readFromConsoleService** | Ввод пользователя |

## 🛠️ Разработка

### Добавление слов
Отредактируйте `internal/resources/words.txt`:
```
новое_слово
другое_слово
```

### Сборка под разные ОС
```bash
# Windows 64-bit
GOOS=windows GOARCH=amd64 go build -o hangman.exe cmd/main.go

# Linux 64-bit
GOOS=linux GOARCH=amd64 go build -o hangman-linux cmd/main.go

# macOS 64-bit
GOOS=darwin GOARCH=amd64 go build -o hangman-macos cmd/main.go
```

## 📝 Лицензия
Лицензировано обществом несмешных приколов

## ⚠️ Примечание для Windows
Если возникают проблемы с регистром файлов, выполните:
```bash
git config core.ignorecase true
```